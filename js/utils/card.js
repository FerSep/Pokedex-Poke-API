/*
export async function card(name, index) {
    document.getElementById('main').innerHTML +=`
        <div class="card">
        <span>${index}</span>
        <h2>${name}</h2>
        </div>
        `;
}
*/

